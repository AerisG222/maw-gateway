server {
    listen [::]:80;
    listen 80;

    server_name staging-photos.mikeandwan.us;

    return 301 https://staging-photos.mikeandwan.us:8443$request_uri;
}

server {
    listen [::]:443 ssl;
    listen 443 ssl;
    http2 on;

    server_name staging-photos.mikeandwan.us;

    root /dev/null;

    access_log /var/log/nginx/maw_photos.access_log main;
    error_log  /var/log/nginx/maw_photos.error_log;

    include tls.conf;

    ssl_trusted_certificate /certs/maw-ca.crt;
    ssl_certificate         /certs/maw-photos.crt;
    ssl_certificate_key     /certs/maw-photos.key;
    ssl_dhparam             /certs/dh2048.pem;

    location / {
        proxy_pass https://localhost:4200;
    }
}
